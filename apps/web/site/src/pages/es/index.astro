---
import { APP_NAME } from '../../content/brand';
import LandingPage from '../../components/landing-page.astro';
import { getSiteContent } from '../../content/site-content';
import BaseLayout from '../../layouts/base-layout.astro';

const copy = getSiteContent('es');
---

<BaseLayout
  lang="es"
  locale="es"
  page="home"
  title={`${APP_NAME} | Un origen con límites explícitos de runtime y confianza`}
  description={copy.description}
>
  <LandingPage locale="es" />
</BaseLayout>
