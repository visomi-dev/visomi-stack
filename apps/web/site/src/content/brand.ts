import template from '../../../../../template.json';

export const APP_NAME = template.project.name;
export const ORGANIZATION = template.project.organization;
export const DEFAULT_LOCALE = template.project.defaultLocale === 'es' ? 'es' : 'en';
